import java.util.Hashtable;

public class ContactManagement {
    private Hashtable<Integer, Contact> contacts = new Hashtable<>();
    public void addContact(Contact contact) {
        contact(contact.getId(), contact);
        contact.
    }
}
